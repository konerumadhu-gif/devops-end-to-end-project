# devops-end-to-end-project
Production-style DevOps pipeline with Dockerized app, Kubernetes deployment, Terraform infrastructure, and GitHub Actions CI/CD
# Sample Flask App

This is a simple Flask-based API used for demonstrating DevOps workflows.

## Endpoints

- `/` → Returns hello message  
- `/health` → Health check endpoint  

## Run locally

```bash
pip install -r requirements.txt
python app.py
